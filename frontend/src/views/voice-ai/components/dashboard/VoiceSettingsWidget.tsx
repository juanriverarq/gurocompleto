import React from 'react';
import VoiceSettings from '../VoiceSettings';

const VoiceSettingsWidget = () => {
  return (
    <div>
      <VoiceSettings />
    </div>
  );
};

export default VoiceSettingsWidget; 